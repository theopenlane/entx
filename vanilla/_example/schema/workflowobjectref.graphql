# WorkflowObjectRef's GraphQL query field is generated by entgql.QueryField() directly into
# ent.graphql; this file exists only so genhooks.GenSchema doesn't overwrite it with its default
# full-CRUD stub (see genhooks/genschema.go: it skips generation if the file already exists)
