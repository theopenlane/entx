// Package entityops generates shared entity operation helpers — create, update,
// upsert, query, and field-update — with typed schema descriptors, Gala event
// infrastructure, and shared operation metadata. The generated output is consumed
// by both the workflows and integrations packages.
package entityops
